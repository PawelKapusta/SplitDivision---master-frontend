import styled from "styled-components";

export const LogoContainer = styled.div`
    max-width: 200px;
    margin: 0 auto;
    border-radius: 45px;
    filter: brightness(120%);

    img {
        display: block;
        border-radius: 45px;
    }
`;
