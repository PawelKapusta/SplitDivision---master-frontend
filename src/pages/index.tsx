import React from "react";
import { Layout } from "../layout/layout";

const Home: React.FC = () => {
    return (
        <Layout>
            <div style={{ height: "100vh" }}>Home page</div>
        </Layout>
    );
};

export default Home;
