import React from "react";
import { Layout } from "../layout/layout";

const Home: React.FC = () => {
    return (
        <Layout>
            <div>Home page</div>
        </Layout>
    );
};

export default Home;
